# URL del backend NestJS. Para probar en un móvil físico con Expo Go usa la
# IP local de tu ordenador en la red (no "localhost", el móvil no resuelve eso).
# Ejemplo: http://192.168.1.50:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
